{
  "name": "metodo-trader-369",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "serve -s dist -l ${PORT:-8080}",
    "preview": "vite preview"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
